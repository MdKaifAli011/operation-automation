/**
 * @deprecated Import from `@/lib/leadSources` — options are configurable in Settings.
 * Kept for backward compatibility; mirrors default lead sources.
 */
export {
  DEFAULT_LEAD_SOURCE_OPTIONS as DATA_TYPE_PICK_OPTIONS,
  dataTypeToShortLabel,
} from "@/lib/leadSources";
